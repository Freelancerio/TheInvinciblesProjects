// A reusable Header component displayed at the top of the app.
// Right now it just shows a title, but can be expanded with navigation or branding later.


import React from 'react';

function Header() {
  return (
    <header>
      <h2>Header Component</h2>
    </header>
  );
}

export default Header;
