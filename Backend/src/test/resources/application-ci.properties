spring.application.name=Backend-Test

# Testcontainers PostgreSQL configuration for CI
spring.datasource.url=jdbc:tc:postgresql:15:///testdb
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver
spring.datasource.username=test
spring.datasource.password=test

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Server configuration for tests
server.port=0

# Disable Firebase for tests
firebase.enabled=false

# CI-optimized logging configuration (less verbose)
logging.level.com.outh.backend=INFO
logging.level.org.springframework.web=WARN
logging.level.org.springframework.boot.autoconfigure=ERROR
logging.level.org.testcontainers=INFO
logging.level.com.github.dockerjava=WARN
logging.level.org.apache.http=WARN
logging.level.com.zaxxer.hikari=WARN